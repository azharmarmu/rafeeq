import 'animal.dart';

// Polymorphism
// When a child class overrides a method of its parent class, it is called polymorphism.
// Polymorphism is achieved only there if there is inheritance.


void main() {
  // Animal animalObj = Animal();
  // animalObj.eat();

  Dog dogObj = Dog();
  dogObj.eat();
}
